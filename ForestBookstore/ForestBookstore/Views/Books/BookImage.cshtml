@using System.Web.Helpers
@model ForestBookstore.Models.DbContext.Book

@{
    if (Model.Image != null)
    {
        WebImage wi = new WebImage(Model.Image);
        wi.Resize(100, 200, true, true);

        wi.Write();
    }
}

